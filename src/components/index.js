/**
 * export components from index so I don't have to
 * import from each specific file.
 */
export { default as Header } from './Header';
export { default as Matchup } from './Matchup';
export { default as PlayerCard } from './PlayerCard';
export { default as GuessCount } from './GuessCount';
